const Rectangle = require("./0-rectangle");

module.exports = class Rectangle {
  constructor(w, h) {
    this.width = w;
    this.height = h;
  }

  empty_obj() {
    if (this.w || this.h == 0 || this.w || this.h < 0) {
      const myObj = {};
    }
  }
};
